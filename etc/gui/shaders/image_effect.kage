//kage:unit pixels
package main
var Time float      

func Fragment(dstPos vec4, srcPos vec2) vec4 {
    color := imageSrc0At(srcPos)
    osc := (sin(Time)+1.0)/2.0
    color.a = color.a * osc
    if color.a <= 0.1 {
        return vec4(0.0, 0.0, 0.0, 0.0)
    }
    return color
}